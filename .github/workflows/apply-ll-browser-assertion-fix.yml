name: apply-ll-browser-assertion-fix

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    if: false
    runs-on: ubuntu-latest
    steps:
      - name: Completed
        run: echo completed
