# Governance App bootstrap verification

This documentation-only change exists solely to verify that the dedicated Noditech Governance Gate GitHub App authenticates correctly and publishes the required App-sourced check on the pull-request head and test-merge SHA.

No product, A/A, A/L, L/L, M1, build, runtime, release or validation logic is changed.
